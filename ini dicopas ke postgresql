CREATE DATABASE infolaundry;

CREATE TABLE pemilik(
idpem serial,
email varchar(255) not null,
password varchar(16) not null,
nama varchar(64) not null,
sex varchar(10),
kontak varchar(64) not null
);

CREATE TABLE pengguna(
iduser serial,
email varchar(255) not null,
password varchar(16) not null,
nama varchar(64) not null
);

CREATE TABLE Ulasan(
idlau serial not null,
idul serial,
iduser serial not null,
tanggal timestamp without time zone,
rating integer,
review varchar(250)
);

CREATE TABLE laundryan(
idlau serial,
namaus varchar(255),
alamat varchar(255),
kelurahan varchar(64),
kontak varchar(30),
Tarif varchar(64),
rincian varchar(200),
idpem serial not null
);

ALTER TABLE Pemilik ADD CONSTRAINT Pemilik_IDpem_PK PRIMARY KEY(IDpem);
ALTER TABLE Pengguna ADD CONSTRAINT Pengguna_IDuser_PK PRIMARY KEY(IDuser);
ALTER TABLE Laundryan ADD CONSTRAINT Laundryan_IDlau_PK PRIMARY KEY(IDlau),
CONSTRAINT Laundryan_IDpem_FK FOREIGN KEY(IDpem) 
REFERENCES Pemilik(IDpem);
ALTER TABLE Ulasan ADD CONSTRAINT Ulasan_IDul_PK PRIMARY KEY(IDul),
CONSTRAINT Ulasan_IDuser_FK FOREIGN KEY(IDuser) 
REFERENCES Pengguna(IDuser),
CONSTRAINT Ulasan_IDlau_FK FOREIGN KEY(IDlau) 
REFERENCES Laundryan(Idlau);
